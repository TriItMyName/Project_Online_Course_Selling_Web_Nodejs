services:
  frontend:
    build:
      context: ../../FrontEnd
      dockerfile: Dockerfile
    container_name: my-frontend-app
    restart: unless-stopped
    ports:
      - "5000:5000"
    env_file:
      - ../../FrontEnd/.env
